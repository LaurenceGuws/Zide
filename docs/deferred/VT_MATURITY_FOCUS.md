# VT Maturity Focus (Deferred)

Status: deferred as the repo-wide default focus.

This note exists so the old default focus is explicitly paused instead of
competing silently with the active renderer/backend campaign.

## What Changed

VT maturity purity is still important work, but it is no longer the default
cross-session repo priority.

The active default focus is now:

- make OpenGL and Metal the two reference implementations for a best-in-class
  renderer backend abstraction

## When To Resume VT Work

Resume VT maturity as the default focus only when repo authority explicitly
switches back or when the renderer/backend contract campaign reaches a real
validated stopping point.

## Owning Authority When Resumed

- `app_architecture/terminal/VT_MATURITY_PURITY_CAMPAIGN.md`
- `app_architecture/terminal/VT_MATURITY_COMPLETION_LIST.md`
- `docs/todo/terminal/vt_core_rearchitecture.md`
- `docs/todo/terminal/widget_scrutiny.md`
