# Terminal Flutter Adapter Notes

This file documents host-adapter notes for Flutter-style consumers of the
terminal bridge.

It used to live under `app_architecture/terminal/ffi/` while the bridge surface
was still being shaped. It now lives under `docs/reference/` because it is
consumer-facing integration guidance rather than current FFI architecture
authority.
