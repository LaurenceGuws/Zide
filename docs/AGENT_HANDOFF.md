## Handoff (High-Level)

### Current Focus
- Primary: terminal core architecture redesign planning.
  - terminal core review: `app_architecture/review/TERMINAL_CORE_ARCHITECTURE_REVIEW_2026-03-10.md`
  - active terminal core queue: `app_architecture/terminal/vt_core_rearchitecture_todo.yaml`
  - core boundary design: `app_architecture/terminal/VT_CORE_DESIGN.md`
  - later redraw/publication contract target: `app_architecture/terminal/RENDER_PUBLICATION_CONTRACT.md`
  - terminal architecture baseline: `app_architecture/terminal/MODULARIZATION_PLAN.md`
- Current top-of-queue focus:
  - define a real VT core below `TerminalSession`
  - keep FFI first-class and host-agnostic
  - make PTY one transport implementation, not the architectural center
  - current implementation state: `TerminalSession` now wraps an internal `TerminalCore` owner for engine state, with PTY/runtime/threading still session-owned
  - `TerminalSession` construction now also delegates to `session_runtime.zig`, so the root session file is moving toward a host-wrapper facade instead of owning full runtime-state assembly inline
  - replay/test-only debug helpers now also live under `terminal_session_debug.zig`, so that non-runtime surface is no longer embedded in the root session file
  - dead local root-session helper residue is also starting to disappear; the remaining test-facing view-cache shim now delegates through `session_rendering.zig` instead of owning view-cache plumbing inline
  - remaining scroll-refresh and OSC clipboard copy wrappers now also delegate through `session_rendering.zig` and `session_queries.zig` instead of sitting inline in the root session file
  - backlog hinting now also routes through `session_runtime.zig` instead of `session_rendering.zig`, so transport ingress pressure no longer sits in the render/publication owner
  - host-facing metadata, liveness, and close-confirm signal queries now also route through `session_host_queries.zig`, leaving `session_queries.zig` focused on terminal-data query surfaces
  - workspace/runtime input-pressure hinting now also routes through `session_runtime.zig` instead of staying inline in the root session file
  - stale private root-session shims for SGR application and key-mode flag reads are now also gone
  - protocol execution is beginning to split along the same line: pure engine-side helpers now live under `terminal_core_protocol.zig`, while session-coupled protocol behavior remains in `session_protocol.zig`
  - parser/control dispatch entry helpers now also live under `terminal_core_dispatch.zig`; parser byte feed still stays session-owned because locking and publication are not split yet
  - parser feed now runs through `terminal_core_feed.zig` with an explicit `FeedResult` publication contract; the remaining session-owned wrapper is specifically state locking plus delegation of generation/view-cache publication to `session_rendering.zig`
  - saved-cursor restore and alt-screen core state transitions now also live under `terminal_core_modes.zig`; `session_protocol` keeps only the remaining session-owned side effects around those transitions
  - RIS/reset core mutation now also lives under `terminal_core_reset.zig`; `session_protocol` keeps only the input snapshot republish step around reset
  - hyperlink allocation, kitty image clearing, and scroll-region mutation now also live under `terminal_core_protocol.zig`; `session_protocol` is trending toward a publication/selection wrapper rather than another core-mutation owner
  - those remaining alt-screen/reset session-only side effects now also live under `session_mode_effects.zig`, so `session_protocol` is closer again to a dispatch facade over core helpers plus explicit session effect owners
  - alt-screen exit presentation timing now also routes through `session_rendering.zig`, so mode-side-effect code no longer mutates render/publication timing state inline
  - first internal transport contract now exists in `terminal_transport.zig`; PTY lifecycle/resize/aliveness/exit/foreground-process metadata are routed through it
  - writer path is now transport-owned via `terminal_transport.Writer`
  - read/poll byte pumps now also route through `terminal_transport.Transport.read(...)` and `waitForData(...)`
  - low-risk writable-transport presence gates now also go through `terminal_transport` helpers
  - PTY attach/detach setup now also goes through `terminal_transport`
  - OSC 5522 protocol replies now also use the transport writer guard; the main remaining PTY-direct area is narrower transport-shape residue
  - first non-PTY transport shape now exists too: FFI-created no-PTY sessions attach an in-memory external transport, and `feed_output` now enters through that transport + `poll()` rather than bypassing transport ownership
  - that external transport path is now also covered by core tests, including explicit alive/closed semantics
  - replay fixtures now also use the external transport path for normal non-reply cases, so the transport is no longer only exercised through FFI hosts
  - higher-level callers now use session-owned wrapper methods for external transport attach/enqueue/close instead of touching `terminal_transport` directly for that path
  - reply-capture PTY attachment in replay now also goes through the same session-owned wrapper style
  - higher-level session assembly now consistently chooses PTY vs external transport through `TerminalSession` wrapper methods rather than raw `terminal_transport` attach/detach calls
  - terminal FFI now also exposes `close_input(...)` plus a derived `alive_changed` event for no-PTY hosts, so embedded consumers can observe external-input closure without keeping the terminal alive forever by polling metadata only
  - terminal FFI now also emits a derived `redraw_ready` event when snapshot generation advances, giving embedded hosts a minimal wake signal for pulling fresh snapshots without introducing a second damage model
  - host-side FFI resize now also participates in that same `redraw_ready` contract, so PTY-backed foreign hosts and no-PTY embedded hosts share the same wake rule after visible size changes
  - reference-repo read against Ghostty and Alacritty now informs the host wake rule too: keep `start(...)` poll-driven, but emit redraw wake on actual visible-state changes such as streamed output, PTY poll-driven updates, and resize
  - the input-mode query/toggle surface now also routes through `session_interaction.zig`, shaving another non-runtime API cluster off the root `TerminalSession` facade
  - the FFI-first split now has explicit internal owners: shared ABI/state/helpers in `src/terminal/ffi/shared.zig`, PTY-host operations in `src/terminal/ffi/host_api.zig`, core-facing export logic in `src/terminal/ffi/core_api.zig`, and `bridge.zig` as a thin facade
- Repo-structure cleanup is complete enough; do not keep grinding non-product cleanup unless a new concrete smell appears.
- Terminal correctness cleanup remains the quality bar, but the current active lane is architectural center-of-gravity correction.

### Recent Changes (High-Level)
- The high-refresh rain investigation removed most renderer-side force-full and stale invalidation escape hatches.
- Full-screen `ascii-rain` is now close to stable, so the stronger remaining work is structural rather than incident-driven.
- `src/app` dumping-ground cleanup materially moved forward: terminal, search, editor, and tabs now have focused app subtrees instead of a flat root namespace.
- `src/editor/editor.zig` large-file cleanup is active: search worker/highlighter logic now lives under `src/editor/search_highlight.zig`, selection/multi-caret/undo-selection state under `src/editor/selection_state.zig`, cursor navigation/selection-extension behavior under `src/editor/navigation.zig`, and text mutation flows under `src/editor/edit_ops.zig`, with `editor.zig` shrinking toward a real facade over editor state ownership.
- `src/editor/syntax.zig` large-file cleanup is effectively landed: syntax query loading/capture mapping/query-cache ownership now live under `src/editor/syntax_queries.zig`, runtime highlighter/injection execution under `src/editor/syntax_runtime.zig`, predicate/directive evaluation plus node-text/regex helpers under `src/editor/syntax_predicates.zig`, and token overlap post-processing under `src/editor/syntax_tokens.zig`, leaving `syntax.zig` as a small root facade instead of another mixed-responsibility file.
- `src/ui/widgets/terminal_widget_draw.zig` large-file cleanup is now active: the grid/text rendering owner has moved into `src/ui/widgets/terminal_widget_draw_grid.zig`, which now owns row background drawing, shaping runs, underline emission, sprite/box glyph rendering, and glyph-cache submission. The root draw file now leans more toward texture-update orchestration, overlays, cursor, selection, and scrollbar chrome.
- `src/ui/widgets/terminal_widget_draw.zig` large-file cleanup has moved forward again: overlay/chrome ownership now also lives in `src/ui/widgets/terminal_widget_draw_overlay.zig`, covering selection overlays, hover underline, cursor drawing, IME composition preview, scrollbar chrome, and the scrollback badge. The root file is now primarily texture-update orchestration and the remaining kitty/presentation glue.
- `src/ui/widgets/terminal_widget_draw.zig` large-file cleanup is effectively landed: grid/text rendering now lives in `src/ui/widgets/terminal_widget_draw_grid.zig`, overlay/chrome in `src/ui/widgets/terminal_widget_draw_overlay.zig`, and viewport-shift/texture-update/partial redraw planning in `src/ui/widgets/terminal_widget_draw_texture.zig`. The root file is now coordinator-sized, with only top-level kitty/presentation/perf glue left in place.
- `src/ui/widgets/editor_widget_draw.zig` large-file cleanup is now active: draw-list primitives, selection/search overlays, extra caret helpers, selection-state hashing, and scrollbar rendering now live in `src/ui/widgets/editor_widget_draw_overlay.zig`, leaving the root file more focused on text/layout/highlight assembly plus cached/live draw orchestration.
- `src/ui/widgets/editor_widget_draw.zig` cleanup moved forward again: byte-range mapping, selection-text background splitting, token color selection, and highlighted segment rendering now live in `src/ui/widgets/editor_widget_draw_text.zig`. The root file is now largely the draw entrypoint/coordinator plus precompute/hash/fallback helpers.
- `src/ui/widgets/editor_widget_draw.zig` cleanup is now effectively landed: draw-list/overlay/scrollbar helpers live in `src/ui/widgets/editor_widget_draw_overlay.zig`, text/highlight segment assembly in `src/ui/widgets/editor_widget_draw_text.zig`, and fallback/precompute/hash planning in `src/ui/widgets/editor_widget_draw_cache.zig`. The root file is now coordinator-sized instead of another large mixed-responsibility draw file.
- `src/ui/renderer.zig` large-file cleanup is now active: SDL/input event polling, event dispatch, and frame queue compaction now live in `src/ui/renderer/input_runtime.zig`, leaving the root renderer file thinner around top-level renderer orchestration instead of also owning the full input runtime loop inline.
- `src/ui/renderer.zig` cleanup moved forward again: font-feature config plus UI-scale/zoom runtime now live in `src/ui/renderer/font_runtime.zig`, leaving the root renderer file thinner around rendering entrypoints instead of also carrying font runtime policy inline.
- `src/ui/renderer.zig` cleanup moved forward again: text/glyph rendering, shaping, and terminal cell draw paths now live in `src/ui/renderer/text_runtime.zig`, leaving the root renderer file thinner around draw coordination and backend wrappers instead of also owning the heaviest text rendering internals inline.
- `src/ui/renderer.zig` cleanup is now effectively landed: SDL/input event polling lives in `src/ui/renderer/input_runtime.zig`, font-feature config plus UI-scale/zoom runtime in `src/ui/renderer/font_runtime.zig`, and text/glyph rendering in `src/ui/renderer/text_runtime.zig`. The root file is now coordinator-sized rather than another large mixed-responsibility renderer blob.
- `src/ui/terminal_font.zig` cleanup is now active: atlas allocation/upload/compaction now lives in `src/ui/font/atlas.zig`, establishing `src/ui/font/` as the focused font subsystem root and starting the split of the remaining font selection/shaping/fallback owners out of the old giant file.
- `src/ui/terminal_font.zig` cleanup moved forward again: preferred/fallback face selection policy now lives in `src/ui/font/fallback.zig`, reducing the root font file around shaping and system font discovery instead of also carrying the full fallback preference matrix inline.
- `src/ui/terminal_font.zig` cleanup moved forward again: special-glyph sprite generation, powerline outline rasterization, and sprite-cache population now live in `src/ui/font/special_glyphs.zig`, reducing the root font file around regular glyph shaping/draw flow and system font discovery instead of also carrying the full special-glyph sprite path inline.
- `src/ui/terminal_font.zig` cleanup moved forward again: regular glyph shaping, grapheme shaping, glyph-advance lookup, and draw placement now live in `src/ui/font/shaping.zig`, reducing the root font file around face selection and system font discovery instead of also carrying the full HarfBuzz draw path inline.
- `src/ui/terminal_font.zig` cleanup is now effectively landed: system font discovery, FreeType face loading, and platform fallback resolution now live in `src/ui/font/system_fallback.zig`, with atlas ownership in `src/ui/font/atlas.zig`, face preference policy in `src/ui/font/fallback.zig`, special glyph sprite handling in `src/ui/font/special_glyphs.zig`, and shaping/draw flow in `src/ui/font/shaping.zig`. The root `terminal_font.zig` file is now coordinator-sized rather than another mixed-responsibility font blob.
- `src/config/lua_config_ziglua_parse.zig` cleanup is still active: theme parsing lives in `src/config/lua_config_theme_parse.zig`, keybind parsing in `src/config/lua_config_keybind_parse.zig`, log/filter parsing in `src/config/lua_config_log_parse.zig`, and font/rendering parsing in `src/config/lua_config_font_parse.zig`, leaving the root config parser focused on the remaining scalar/runtime domain groups instead of another broad mixed parser.
- `src/config/lua_config_ziglua_parse.zig` cleanup is effectively done: theme parsing lives in `src/config/lua_config_theme_parse.zig`, keybind parsing in `src/config/lua_config_keybind_parse.zig`, log/filter parsing in `src/config/lua_config_log_parse.zig`, font/rendering parsing in `src/config/lua_config_font_parse.zig`, and runtime option/helper parsing in `src/config/lua_config_runtime_parse.zig`. The root config parser is now a smaller entrypoint/facade instead of another structural hotspot.
- `src/ui/renderer/` micro-file cleanup is now active: the trivial input-state helper files for key state, mouse state, wheel accumulation, key queue pop, input drain, and text composition no longer live as standalone files; that logic now sits with `input_state.zig`, `input_runtime.zig`, and the root renderer facade, reducing file-count drift without collapsing any real renderer subsystem boundaries.
- `src/ui/renderer/` micro-file cleanup moved forward again: clipboard copy/logging helpers no longer live in a separate `clipboard_state.zig` wrapper; they now sit with the SDL clipboard bridge in `clipboard.zig`, removing another low-value navigation hop without merging unrelated renderer behavior.
- `src/ui/renderer/` micro-file cleanup moved forward again: resize/close event classification no longer lives in a separate `window_flags.zig` wrapper; that logic now sits with the SDL input loop in `input_runtime.zig`, removing another low-value renderer wrapper file.
- `src/ui/renderer/` micro-file cleanup moved forward again: begin-frame window/drawable size refresh no longer lives in a separate `window_metrics_state.zig` wrapper; that logic now sits directly in `renderer.zig`, removing another renderer-local one-call-site file.
- `src/ui/renderer/` micro-file cleanup is effectively done: the low-value local wrappers are gone, and the remaining small renderer files now correspond to real reusable helpers or narrow adapter boundaries rather than pure navigation overhead.
- `src/terminal/core/session_*.zig` micro-file cleanup is now active: the first low-value collapse merged `session_clipboard.zig` into `session_interaction.zig`, because paste fallback policy, bracketed paste handling, and OSC 5522 paste-event plumbing were already one cohesive interaction surface and the extra file only added navigation overhead.
- `src/terminal/core/session_*.zig` micro-file cleanup moved forward again: `session_publication.zig` was collapsed into `session_rendering.zig`, because generation tracking, backlog hints, presented-generation acknowledgement, and presentation feedback were already one presentation lifecycle surface and the separate file no longer bought a meaningful boundary.
- `src/terminal/core/session_*.zig` micro-file cleanup is now effectively landed: the remaining `session_*` files still correspond to real owners (`input`, `protocol`, `rendering`, `runtime`, `selection`, `queries`, `content`, `config`), so further collapse there would re-mix domains rather than improve the layout.
- `src/config/lua_config_ziglua_parse.zig` cleanup is now active: theme/color/editor-theme parsing now lives in `src/config/lua_config_theme_parse.zig`, leaving the root config parser more focused on scalar option parsing and keybind parsing instead of also carrying the full theme parser inline.
- `src/config/lua_config_ziglua_parse.zig` cleanup moved forward again: keybind parsing now lives in `src/config/lua_config_keybind_parse.zig`, leaving the root config parser more focused on scalar/domain option parsing instead of also carrying the full keybind parser inline.
- `src/config/lua_config_ziglua_parse.zig` cleanup moved forward again: log/filter/SDL-log parsing now lives in `src/config/lua_config_log_parse.zig`, leaving the root config parser more focused on the remaining scalar domain config groups instead of also carrying the full logging parser inline.
- `src/terminal/core/terminal_session.zig` has started shrinking again: its large regression block now lives in `src/terminal/core/terminal_session_tests.zig`, so the next worthwhile terminal-heavy file cuts can focus on runtime ownership in `terminal_session.zig`, CSI reply/query ownership in `src/terminal/protocol/csi.zig`, and edit/scroll mutation ownership in `src/terminal/model/screen/screen.zig`.
- `src/terminal/model/screen/screen.zig` has also started shrinking again: the edit/erase/scroll mutation block now lives in `src/terminal/model/screen/edit_ops.zig`, cursor/margin/tab/newline navigation now lives in `src/terminal/model/screen/navigation_ops.zig`, and codepoint/ascii/write-prep handling now lives in `src/terminal/model/screen/write_ops.zig`, leaving the remaining root screen weight concentrated in shared screen-state ownership rather than generic movement/edit/write plumbing.
- `src/terminal/protocol/csi.zig` has now started shrinking again too: writer/query contexts and DA/DSR/window-op reply formatting now live in `src/terminal/protocol/csi_reply.zig`, DECRQM snapshot/state handling now lives in `src/terminal/protocol/csi_mode_query.zig`, simple/special execution now lives in `src/terminal/protocol/csi_exec.zig`, `SM`/`RM` mode mutation policy now lives in `src/terminal/protocol/csi_mode_mutation.zig`, and SGR/DECSTR style-reset ownership now lives in `src/terminal/protocol/csi_style_reset.zig`. The remaining root CSI weight is now mostly top-level dispatch glue.
- `TerminalSession` surface reduction is largely landed: borrowed title/cwd/scrollback/selection/input/interaction/rendering/protocol/config/runtime seams were cut back, terminal text export now lives in backend code instead of the widget, FFI/workspace/open-path callers use backend-owned metadata/export contracts instead of separate raw getter calls, split title/cwd helpers, or a dedicated child-exit getter, terminal-data query logic now lives under `src/terminal/core/session_queries.zig`, host metadata/liveness/close-confirm query logic under `src/terminal/core/session_host_queries.zig`, scrollback/text-export/scroll-offset content helpers under `src/terminal/core/session_content.zig`, selection passthrough helpers under `src/terminal/core/session_selection.zig`, key/char/text/mouse/focus/color-scheme send paths under `src/terminal/core/session_input.zig`, input-state query plus OSC 5522 paste helpers under `src/terminal/core/session_interaction.zig`, snapshot/render-cache/sync-updates damage-retirement plus generation/backlog/presentation-feedback helpers under `src/terminal/core/session_rendering.zig`, parser/control/screen-edit/alt-screen/DECRQSS protocol helpers under `src/terminal/core/session_protocol.zig`, palette/theme/column-mode/cell-size config helpers under `src/terminal/core/session_config.zig`, and PTY startup/poll/child-exit/write-lock/resize runtime helpers under `src/terminal/core/session_runtime.zig` instead of sitting inline in `terminal_session.zig`.
- `TerminalSession` residue cleanup also moved forward: the stale duplicate DECRQSS SGR formatting helpers were removed from `terminal_session.zig`, leaving `src/terminal/core/session_protocol.zig` as the single owner for DECRQSS reply assembly instead of carrying two parallel implementations.
- `TerminalSession` lifecycle ownership also moved forward: the broad session teardown block now lives in `src/terminal/core/session_runtime.zig`, so `terminal_session.zig` no longer owns PTY/thread shutdown and resource destruction inline.
- Workspace/session boundary tightening is now active: app clipboard shortcuts no longer resolve sessions through workspace, the dead app-side active-session resolver was deleted, workspace tab creation now has an explicit `createTabWithSession(...)` contract, the remaining raw workspace session helpers (`sessionAt`, `activeSession`) were pushed back to internal-only helpers, terminal tab-bar sync now consumes one workspace-owned `copyTabSyncState(...)` contract, and window-close confirm routing now uses `firstConfirmCloseTab(...)` instead of app-side tab scanning.
- Runtime scheduling ownership cleanup is now effectively landed: workspace owns active-frame observation (`activeFrameState()`) and per-frame poll publication (`PollFrameResult`), runtime owns poll profile selection and sleep cadence through explicit runtime profiles, and terminal idle/backoff bookkeeping lives inside `TerminalFramePacingState` instead of generic `AppState` fields.
- Widget-input reduction also moved forward: terminal scrollbar drag state is now widget-owned instead of living in generic app state/glue, and click/drag/scroll gestures now lean on backend-owned selection/scroll helpers instead of duplicating row/word/anchor policy in `terminal_widget_input`.
- Widget/backend presentation ownership also moved forward: terminal widget draw now goes through backend-owned presentation capture/finish methods instead of assembling published-cache copy timing and feedback handoff inline in the widget.
- Clipboard/paste ownership also moved forward: system and middle-click selection paste fallback policy now lives in backend code instead of widget code, including scrollback reset for system paste, OSC 5522 attempt, bracketed paste framing, and plain-text fallback behavior.
- Scrollback-view collapse policy also moved forward: widget input no longer owns the actual “snap back to live bottom on input activity” rule; that decision now goes through backend scrollback helpers.
- Terminal widget input ownership also moved forward: the keyboard/text dispatch path, the locked pointer/selection/scrollback path, and the mouse-reporting path now live in dedicated UI helper modules instead of staying embedded inside `terminal_widget_input.zig`, making the widget input file a top-level coordinator over helper modules rather than another large mixed-responsibility block.
- Investigation residue cleanup is active: probe-grade scroll/control trace logging was removed from core hot paths, protocol trace noise now defaults to debug level, high-frequency input/scrollback send-path logs were demoted out of default-info output, raw CSI-byte tracing now defaults to debug, and duplicate stdout lifecycle logs were removed.
- Current high-risk architectural seams are:
  - `TerminalSession` is still a large multi-domain owner (PTY/parser/screens/history/render publication/UI-facing APIs).
  - terminal-originated PTY writes and the main session mutation/publication locking cleanup are now in better shape, so the next structural hotspot is the oversized `TerminalSession` surface and the borrowed app/UI query seams hanging off it.
  - redraw lifecycle ownership is significantly cleaner: published cache capture and post-draw completion are now behind backend APIs, although `view_cache`, widget wrapper code, and frame runtime still participate in the presentation pipeline.
  - scheduler/poll state is still split across app runtime helpers and `TerminalWorkspace`, but concrete workspace poll budgets now live behind the workspace contract instead of the app hook.
- protocol/parser seam is still only partially typed; the VT parser callback contract in `parser.zig`, top-level parser dispatch in `parser_hooks`, DCS/APC/OSC/CSI parser dispatch, OSC title/hyperlink/cwd/clipboard/semantic/palette/kitty-clipboard boundaries, and kitty paste-event emission/internal helper paths now run through explicit session facades/context structs (including OSC 5522 write/reply flow through an explicit `WriterFacade`). In parser text-write handling, codepoint/ascii execution in `parser_hooks` now runs through an explicit text-write context instead of reaching through raw session shape. OSC top-level dispatch now routes through a typed composite facade over the split OSC submodules instead of another opaque trampoline layer, and the helper modules for title, hyperlink, cwd normalization, OSC 52 clipboard, semantic prompt/user vars, and palette/dynamic colors now use direct typed state holders wherever the logic already owns that state instead of layering wide per-call opaque callback facades on top. In CSI, reply emission now runs through `CsiWriter`, query/reply data through `QueryContext`, screen cursor/grid reply reads through `ScreenQueryContext`, simple cursor/edit/scroll execution through `SimpleCsiContext`, `SM`/`RM` mode toggles through `ModeMutationContext`, `DECRQM` snapshot capture through `ModeQueryContext`, the remaining simple special-case control branches through `SpecialCsiContext`, and reply/control lock-dispatch orchestration through `ReplyCsiContext`; `DECSTR` now runs through an explicit reset context and `applySgr` through an explicit SGR context. `dcs_apc` also no longer bounces kitty APC payloads back through a public `TerminalSession.parseKittyGraphics(...)` wrapper. Remaining implicit seams are narrower but still exist in selected CSI policy corners, protocol entry wrappers, and some widget/backend boundaries.
- Kitty graphics is no longer a single mixed-responsibility blob. Shared kitty state/types, payload transport/build, placement-graph mutation/scroll/placement helpers, byte-store lifecycle, and protocol/delete/reply orchestration now live in dedicated `src/terminal/kitty/common.zig`, `src/terminal/kitty/transport.zig`, `src/terminal/kitty/placement_ops.zig`, `src/terminal/kitty/storage_ops.zig`, and `src/terminal/kitty/protocol_ops.zig` modules. `graphics.zig` is now mostly a thin facade/re-export surface plus tests.
  - widget input/draw still contain backend-policy behavior rather than being thin presentation/orchestration layers.
  - selection semantics are cleaner than before because row-content/word/range rules now live in backend model/core code, but the widget still owns substantial interaction orchestration and presentation policy.

### Constraints / Guardrails
- Handoff docs remain high-level only; details belong in `app_architecture/*` docs and todo files.
- This repo intentionally has no CI; do not add CI workflows.
- Agent owns `./.zide.lua` logging scope during debugging (minimal useful tags; low noise).
- `main` is the default branch. Feature branches are for larger isolated cuts only, and the agent owns creating, merging, and deleting them.
- Do not keep dead seams or compatibility wrappers just to avoid a hard cut when the old surface is holding the terminal back.

### Where to Look
- Primary file/folder layout review: `app_architecture/review/FILE_LAYOUT_HOTSPOTS_REVIEW.md`
- `src/app` placement authority + move order: `app_architecture/review/SRC_APP_DOMAIN_MAP.md`
- Active file/folder cleanup queue: `app_architecture/file_layout_todo.yaml`
- Primary architecture review + recent cleanup history: `app_architecture/review/TERMINAL_240HZ_RAIN_INVESTIGATION.md`
- Terminal architecture plan + current hotspot list: `app_architecture/terminal/MODULARIZATION_PLAN.md`
- Terminal damage/dirty background + redraw/publication hotspot list: `app_architecture/terminal/DAMAGE_TRACKING.md`
- Future redraw/publication lane contract target: `app_architecture/terminal/RENDER_PUBLICATION_CONTRACT.md`
- Current UI/backend seam tracker: `app_architecture/ui/ui_widget_modularization_todo.yaml`
- Doc workflow policy: `docs/WORKFLOW.md`

### Known Risk (High-Level)
- A broad refactor done in the wrong order will re-entangle correctness with redraw/scheduler changes and make regressions hard to localize.
- The main remaining risk is boundary drift: session/runtime/widget/protocol code still share responsibilities that should be isolated.
- Kitty graphics, sync/presentation ownership, and input snapshot publication remain sensitive correctness surfaces.
