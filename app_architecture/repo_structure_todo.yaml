version: 1

items:
  - id: REPO-TEST-01
    title: Define explicit repo-wide testing layout
    status: done
    priority: high
    notes:
      - Canonical split is now documented between src-adjacent tests, tests/, and fixtures/
      - tests/ root has been established with the aggregate test entrypoint

  - id: REPO-TOOLS-01
    title: Collapse duplicated tooling roots
    status: done
    priority: high
    notes:
      - Removed src/tools as a parallel tooling root
      - Grammar maintenance tools now live under tools/

  - id: REPO-DOC-01
    title: Classify active versus historical documentation
    status: done
    priority: high
    notes:
      - Define what belongs in docs/, app_architecture/, and app_architecture/review/
      - Identify stale completed docs/todos for archival or deletion
      - Doc lifecycle policy is now explicitly recorded in docs/WORKFLOW.md and summarized in docs/INDEX.md

  - id: REPO-TEST-02
    title: Re-home scattered multi-module Zig test entrypoints
    status: done
    priority: medium
    notes:
      - Apply the REPO-TEST-01 policy
      - Prefer reviewable, category-based moves instead of one large migration
      - First slice landed: generic root-level app/editor/config/layout/widget test entrypoints moved from src/ into tests/
      - Second slice landed: standalone terminal root test entrypoints moved from src/ into tests/, while tightly coupled subsystem tests remain source-adjacent
      - Remaining src-adjacent exceptions are now documented explicitly in tests/README.md

  - id: REPO-DOC-02
    title: Remove or archive stale docs and stale completed todos
    status: in_progress
    priority: medium
    notes:
      - Run only after active-vs-historical classification is documented
      - First cleanup slice should target obvious placement/naming outliers before broader stale-doc deletion
      - First cleanup slice landed: moved the old root-level terminal/editor FFI host migration beta todo into app_architecture/terminal/ with stable naming
      - Second cleanup slice landed: moved mode-layering validation and binary-size baseline records out of top-level docs/ into app_architecture/review/
      - Third cleanup slice landed: removed redundant docs/AGENT_HOVER.md so there is one authoritative high-level handoff doc
