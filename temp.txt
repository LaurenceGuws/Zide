Check basic chrome:

  Done          - Top bar is visible in editor-only mode.
  Not working   - File and Edit menus open on click. 
  Done          - Status bar is visible.
  Done          - Current line/column updates as the cursor moves.

  Check file flow:

  Done          - File -> New creates a fresh untitled editor.
  Done          - File -> Open... opens the status-bar path prompt.
  Done          - Entering a valid path in Open... opens that file.
  Done          - File -> Save saves an existing file without prompting. 
    -> Editing our readme is very slow, tyinging is sluggish
  Done          - File -> Save on an untitled buffer opens Save As.
  Done          - File -> Save As... opens the status-bar path prompt.
  Done          - Entering a destination path in Save As... writes the file and clears dirty state.

  Check dirty/file state:

  Done          - Editing text marks the buffer modified.
  Done          - Saving clears the modified indicator.
  Done          - Opened file path is shown in the status bar.
  Done          - Untitled/new buffer does not pretend to have a file path.

  Check find/replace:

  Done          - Edit -> Find opens the find UI.
  Done          - Typing a query updates matches.
  Done          - Enter/F3 moves to next match.
  Done          - Edit -> Replace with an active search opens the replace prompt. 
  Done          - Entering replacement text replaces the active match.
  Done          - Edit -> Replace with no search query falls back to Find.
  We need a replace all mode, and preview replaced text live

  Check keyboard expectations:

  Done          - Ctrl+N creates a new editor. 
    -> the tab chips suck atm tough, we need to some how get it to terminal quality without duplicating the code and not comprimising terminal.
  Done          - Ctrl+S saves, or opens Save As for untitled.
  Done          - Ctrl+F opens find.
  Done          - Ctrl+C/X/V work.
  Done          - Ctrl+Z/Y work. 
    -> technically working but very inconsistent and unintuitive

  Check editor-only mode specifically:

  Done          - No terminal UI appears.
    -> the linking phase of editor only seems to be doing a lot but it might just be all the tree sitter stuff
  Done          - The editor still supports multiple files/tabs if you open/new more than one.
    -> none of the controls work, already 2 complaints about this above.
  Not sure      - Chrome actions work the same in -Dmode=editor as in normal IDE mode for editor features.
    -> I havent worked in IDE mode in a while

  Check failure handling:

  Done          - Invalid open path does not crash the app.
  Done          - Invalid save path does not crash the app.
  Done          - Cancel with Esc closes the path prompt.
  Dine          - Cancel with Esc closes find.

